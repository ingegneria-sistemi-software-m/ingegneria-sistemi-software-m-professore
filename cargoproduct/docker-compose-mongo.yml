version: '2.1'

services:

  mongodb:
    image: mongo:6.0.4
    container_name: mongodb
    mem_limit: 512m
    ports:
      - "27017:27017"
    command: mongod
##    healthcheck:
##      test: "mongostat -n 1"
##      interval: 15s
##      timeout: 2s
##      retries: 60
      
 

 
## docker-compose -f docker-compose-mongo.yml -p mongo up

 
